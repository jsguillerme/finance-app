export interface ICreditCard {
  id?: string;
  limit_value: string;
  number_card: string;
  owner_card: string;
  expired_date: string;
  current_balance?: string;
  income_balance?: string;
  outcome_balance?: string;
  surname: string;
}